import styled from 'styled-components';

const StyledNav = styled.nav`
  display: flex;
  flex-direction: row;
  gap: 15px;
  font-size: 16px;
  font-weight: 400;
  a {
    color: inherit;
  }
`;

export default StyledNav;
